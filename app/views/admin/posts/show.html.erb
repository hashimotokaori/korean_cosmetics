<div class="container">
  <div class="col-sm-10"><h2>投稿履歴詳細</h2></div>
  <%= form_with model: @post, url: admin_post_path, method: :patch, local: true do |f| %>
    <table class="table table-bordered">
        <tr>
          <th class="table-active" style="width:20%">投稿者</th>
          <td class="link"><%= link_to admin_customer_path do %><%= @post.customer.last_name + @post.customer.first_name %><% end %></td>
        </tr>
        <tr><th class="table-active" style="width:20%">投稿日</th><td><%= @post.created_at.strftime("%Y-%m-%d %H:%M:%S") %></td></tr>
        <tr><th class="table-active" style="width:20%">気になる理由</th><td><%= @post.caption %></td></tr>
        <tr><th class="table-active" style="width:20%">評価</th><td id="post_raty">
        <script>
  $(document).on('turbolinks:load', function() {
    let elem = document.querySelector('#post_raty');
    if (elem == null) return;

    elem.innerHTML = "評価 : "
    let opt = {  
      starOn: "<%= asset_path('star-on.png') %>",
      starOff: "<%= asset_path('star-off.png') %>",
      starHalf: "<%= asset_path('star-half.png') %>",
      scoreName: 'post[rate]',
      readOnly: true,
      score: "<%= @post.star %>"
    };
    raty(elem, opt);
  });
</script>
        
        </td></tr>
        <tr><th class="table-active" style="width:20%">感想</th><td><%= @post.review %></td></tr>

          
        </tr>
    </table>
  <% end %>
  
  <div class="row mb-2">
    <div class="col-lg-6 px-sm-0 offset-sm-1">
     
      <%#= link_to "レビューを削除する", admin_posts_path(@post.id), method: :delete, class:"btn btn-primary mx-4 px-4" %>


    </div>
    <div class="row mb-2">
    <div class="col-lg-6 px-sm-0 offset-sm-1">
     
      <%#= link_to "レビューを削除する", admin_posts_path(@post.id), method: :delete, class:"btn btn-primary mx-4 px-4" %>


    </div>
    <div class="row mb-2">
    <div class="col-lg-6 px-sm-0 offset-sm-1">
     
      <%#= link_to "レビューを削除する", admin_posts_path(@post.id), method: :delete, class:"btn btn-primary mx-4 px-4" %>


    </div>
    <div class="row mb-2">
    <div class="col-lg-6 px-sm-0 offset-sm-1">
     
      <%#= link_to "レビューを削除する", admin_posts_path(@post.id), method: :delete, class:"btn btn-primary mx-4 px-4" %>


    </div>
  </div>
  </div>